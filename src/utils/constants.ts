export const API_ENDPOINTS = {
  LISTS: 'lists',
  ITEMS: 'items',
  PROFILES: 'profiles'
} as const;

export const PRIORITIES = {
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high'
} as const;

export const LIST_TYPES = {
  TASKS: 'tasks',
  TEXT: 'text'
} as const;

export const ITEM_TYPES = {
  TASK: 'task',
  TEXT: 'text'
} as const;